[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
dependencies = [
    'pyserial',
    'crccheck',
    'stm32loader',
    'requests',
    'progress
]
build-backend = "setuptools.build_meta"

